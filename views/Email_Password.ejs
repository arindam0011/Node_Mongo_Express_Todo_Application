<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
  <style>
    /* General page styling */
    body {
      margin: 0;
      padding: 0;
      font-family: 'Arial', sans-serif;
      background-color: #2c3e50;
      color: #fff;
    }

    /* Container styling */
    .container {
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      height: 100vh;
      width: 100vw;
      gap: 20px;
      background-color: black;
      /* Slightly lighter background */
      padding: 20px;
      box-sizing: border-box;
    }

    /* Label styling */
    .label {
      font-size: 0.8rem;
      font-weight: bold;
      color: #c7ef27;
      /* Bright yellow */
      margin-bottom: 10px;

    }

    /* Input field styling */
    .input-field {
      padding: 12px 20px;
      font-size: 1rem;
      width: 100%;
      max-width: 300px;
      border: none;
      border-radius: 8px;
      background-color: #2c3e50;
      color: #fff;
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
      transition: all 0.3s ease;
    }

    .input-field:focus {
      outline: none;
      border: 2px solid #c7ef27;
      background-color: #34495e;
    }

    input:-webkit-autofill {
      background-color: #34495e !important;
      color: #fff !important;
    }
    input:autofill {
      background-color: #34495e !important;
      color: #fff !important;
    }

    /* Button styling */
    .submit-btn {
      padding: 12px 30px;
      font-size: 1rem;
      border: none;
      border-radius: 8px;
      background-color: #c7ef27;
      color: #2c3e50;
      cursor: pointer;
      transition: all 0.3s ease;
      width: 100%;
      max-width: 200px;
      margin-inline: auto;
      margin-top: 10px;
    }

    .submit-btn:hover {
      background-color: #f39c12;
      transform: translateY(-2px);
    }

    .submit-btn:active {
      transform: translateY(1px);
    }

    .submit-btn:focus {
      outline: none;
      box-shadow: 0 0 0 2px #eff802;
    }

    .input-field {
      width: 100%;
      padding: 12px;
      font-size: 16px;
      border-radius: 8px;
      border: 1px solid #ccc;
      outline: none;
    }

    .eye-icon {
      position: absolute;
      right: 10px;
      top: 50%;
      transform: translateY(-50%);
      cursor: pointer;
      font-size: 20px;
    }

    @media screen and (max-width: 500px) {
      #child-email-div {
        width: 100% !important;
      }

      #Re-email,
      #New-password {
        width: 90% !important;
      }

      .input-field {
        width: 90% !important;
      }
    }
  </style>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.6.0/css/all.min.css" integrity="sha512-Kc323vGBEqzTmouAECnVceyQqyqdsSiqLQISBL29aUW4U/M7pSPA/gEUZQqv1cwx4OnYxTxve5UMg5GT6L4JJg==" crossorigin="anonymous" referrerpolicy="no-referrer" />
</head>

<body>
  <div id="Re-email-div" class="container">
    <div id="child-email-div" style="display: flex; flex-direction: column; width: 25%; 
        border: #c7ef27 2px solid; padding: 10px; border-radius: 10px; box-sizing: border-box;">
      <label for="Re-email" class="label">Enter your email </label>
      <input type="email" id="Re-email" name="Re-email" class="input-field" placeholder="youremail@example.com"><br>
      <label for="Re-username" class="label">Enter your New Password </label>
      <div class="input-field" style="display: flex; align-items: center; justify-content: space-between; user-select: none;">
        <input type="password" id="New-password" placeholder="Enter your password" style="background-color: #2c3e50; 
        border: none; outline: none; color: #fff; width: 100%; height: 90%;">
        <i id="eye-icon" class="fas fa-eye"></i>
      </div>
      <button id="Re-email-btn" class="submit-btn">Submit</button>
    </div>

  </div>


  <script src="https://unpkg.com/axios@1.6.7/dist/axios.min.js"></script>
  <script src="Newpassword.js">

  </script>
</body>

</html>