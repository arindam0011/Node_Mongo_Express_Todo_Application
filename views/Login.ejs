<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      display: flex;
      justify-content: center;
      align-items: center;
      flex-direction: column;
      width: 100%;
      height: 100vh;
      background: rgb(3, 3, 3);
    }

    #form {
      width: 400px;
      height: auto;
      background-color: #404040;
      border-radius: 20px;
      border: 2px solid #a2a2a3;
      display: flex;
      justify-content: center;
      flex-direction: column;
      padding: 10px;
      box-shadow: 0 0 10px 5px rgba(190, 189, 189, 0.3);
    }

    .h1 {
      margin-bottom: 10px;
      font-family: system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
      font-weight: bold;
      font-style: italic;
      text-align: center;
      color: #c7ef27;
      text-decoration: underline;
      text-underline-offset: 8px;
    }

    .label {
      font-family: 'Lucida Sans', 'Lucida Sans Regular', 'Lucida Grande', 'Lucida Sans Unicode', Geneva, Verdana, sans-serif;
      margin-bottom: 5px;
      font-weight: bold;
    }

    .input {
      margin-bottom: 10px;
      width: 100%;
      height: 40px;
      border-radius: 10px;
      border: none;
      padding-left: 10px;
    }

    .btn {
      width: 100%;
      height: 40px;
      border-radius: 10px;
      border: none;
      outline: none;
      background-color: #c7ef27;
      color: #0a0a0a;
      font-weight: bold;
      cursor: pointer;
      transition: all 0.3s ease;
      margin-inline: auto;
    }

    .btn:hover {
      cursor: pointer;
      width: 80%;
      margin-inline: auto;

    }

    .btn-container {
      display: flex;
      justify-content: center;
    }
  </style>
</head>

<body>
  <form id="form" action="/login" method="POST">
    <h1 class="h1">Login</h1>
    <label class="label" for="loginId">Login ID</label>
    <input class="input" type="text" id="email" name="loginId" placeholder="Email or Username">
    <label class="label" for="password">Password</label>
    <input class="input" type="password" id="password" name="password">
    <div class="btn-container">
        <input class="btn" type="submit" value="Submit">
    </div>
    <p style="text-align: center; margin-top: 10px; color: #f9f8f4">Don't have an account? <a style="color: #f9ec3b;" href="/registration">Register</a></p>
  </form>
</body>

</html>